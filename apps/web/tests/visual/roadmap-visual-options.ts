export const roadmapVisualOptions = {
  referenceProjectionCss: `.roadmap-page {
    --roadmap-footer-ink: var(--ink-mute) !important;
    --roadmap-recommendation-ink: var(--blueprint) !important;
  }`,
} as const;
